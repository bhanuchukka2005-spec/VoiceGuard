# VoiceGuard v2.2 — Python dependencies
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9      # required for FastAPI file uploads

# Audio processing
librosa>=0.10.0
soundfile>=0.12.1
numpy>=1.24.0
pydub>=0.25.1                # universal audio format support (delegates to ffmpeg)

# ML
scikit-learn>=1.4.0
xgboost>=2.0.0
joblib>=1.3.0

# Pydantic (FastAPI dependency — pin v2 for compatibility)
pydantic>=2.0.0